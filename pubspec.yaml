name: widgets_testing
description: A new Flutter application where I can play with widgets
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.0.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
#  google_maps_flutter: ^2.0.1
  yandex_mapkit: ^1.0.0
  cupertino_icons: ^1.0.0
  geolocator: ^7.0.3
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true